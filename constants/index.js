const contractAddresses = require("./contractAddresses.json");
const abi = require("./abi.json");
const abiLinkToken = require("./abiLinkToken.json");
const contractAddressLink = require("./contractAddressLink.json");

module.exports = {
  contractAddresses,
  abi,
  abiLinkToken,
  contractAddressLink,
};
